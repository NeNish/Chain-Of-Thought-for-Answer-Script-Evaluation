[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codemix-hatespeech-transformer"
version = "0.1.0"
description = "Multilingual transformer training pipeline for Tanglish/Hinglish/Kanglish hate speech detection"
requires-python = ">=3.10"
